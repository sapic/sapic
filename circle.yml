machine:
  node:
    version: 8.9
dependencies:
  cache_directories:
    - node_modules
  override:
    - /usr/local/bin/yarn

test:
  override:
    - rm -rf out
    - mkdir out
    - git clone https://github.com/SAPIC/SAPIC.github.io ./:
        pwd: out
    - CIRCLE_BUILD_NUM=${CIRCLE_BUILD_NUM} /usr/local/bin/yarn build
    - scp -r ./* circleci@35.188.219.150:~/www:
        pwd: out
